<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE hibernate-configuration PUBLIC
        "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
        "http://www.hibernate.org/dtd/hibernate-configuration-3.0.dtd">
<hibernate-configuration>
    <session-factory>
        <!-- Database connection settings -->
        <property name="connection.driver_class">org.postgresql.Driver</property>
        <property name="connection.url">jdbc:postgresql://localhost:5432/spacex</property>
        <property name="connection.username">postgres</property>
        <property name="connection.password">database</property>

        <!-- JDBC connection pool settings -->
        <property name="connection.pool_size">5</property>

        <!-- SQL dialect -->
        <property name="dialect">org.hibernate.dialect.PostgreSQLDialect</property>

        <!-- Echo all executed SQL to stdout -->
        <property name="show_sql">true</property>

        <!-- Auto create/update tables -->
        <property name="hbm2ddl.auto">update</property>

        <!-- Entity classes -->
        <mapping class="sfedu.danil.entity.LaunchEntity"/>
        <mapping class="sfedu.danil.entity.LaunchSiteEntity"/>
        <mapping class="sfedu.danil.entity.RocketEntity"/>
        <mapping class="sfedu.danil.entity.SecondStageEntity"/>
        <mapping class="sfedu.danil.entity.PayloadEntity"/>
    </session-factory>
</hibernate-configuration>